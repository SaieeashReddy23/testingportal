import { styled } from 'styled-components'

const Settings = () => {
  return <Wrapper>Settings not yet implemented</Wrapper>
}
export default Settings

const Wrapper = styled.div`
  width: 100%;
  height: 100%;
  display: grid;
  place-items: center;
  font-size: 2rem;
  letter-spacing: 2px;
  font-weight: 500;
  color: var(--grey-500);
`
